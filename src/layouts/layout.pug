//- Стандартный шаблон страницы

include ../blocks/button/button.pug
include ../blocks/burger/burger.pug
include ../blocks/form/form.pug
include ../blocks/field-text/field-text.pug

doctype html

- var lang = "ru";
- var siteName = "Название сайта";

html( class="page no-js", lang="#{lang}" )
  head
    meta( charset="utf-8" )
    meta( name="viewport", content="width=device-width, initial-scale=1" )
    meta( name="language" content="#{lang.toUpperCase()}" )

    //- Edge и ряд мобильных браузеров слишком умны. Отключим это.
    meta( name="format-detection", content="telephone=no" )
    meta( name="format-detection", content="date=no" )
    meta( name="format-detection", content="address=no" )
    meta( name="format-detection", content="email=no" )
    meta( content="notranslate", name="google" )

    //- Фавиконки (https://realfavicongenerator.net)
    //- link( rel="apple-touch-icon", sizes="180x180", href="/favicon/apple-touch-icon.png" )
    //- link( rel="icon", type="image/png", sizes="32x32", href="/favicon/favicon-32x32.png" )
    //- link( rel="icon", type="image/png", sizes="16x16", href="/favicon/favicon-16x16.png" )
    //- link( rel="shortcut icon" type="image/x-icon" href="/favicon/favicon.ico" )
    //- link( rel="manifest", href="/favicon/site.webmanifest" )
    //- link( rel="mask-icon", href="/favicon/safari-pinned-tab.svg", color="#d03030" )
    //- meta( name="application-name" content="#{siteName}" )
    //- meta( name="apple-mobile-web-app-title" content="#{siteName}" )
    //- meta( name="theme-color", content="#fff" )
    //- meta( name="apple-mobile-web-app-status-bar-style", content="#fff" )
    //- meta( name="msapplication-TileColor", content="#fff" )
    //- meta( name="msapplication-config" content="/favicon/browserconfig.xml" )

    block meta
      title Home

    block head

      //- Предзагрузка CDN
      //- link( rel="dns-prefetch" href="//cdnjs.cloudflare.com/" )
      //- link( rel="preconnect" href="//cdnjs.cloudflare.com/" )

      //- Плагин слайдера
      //- link( rel="preconnect" href="//cdnjs.cloudflare.com/ajax/libs/slick-carousel/1.9.0/slick.min.css" )

      link( rel="stylesheet" href="styles/main.min.css" )

  body
    noscript У вас отключен JavaScript. Функциональность сайта ограничена. Пожалуйста, включите JavaScript и перезагрузите страницу.

    div.page__inner
      +page-header()
        block header

      +page-main()
        block main

      +page-footer()
        block footer

    block bottom

      //- Библиотека jQuery
      //- script( src="//cdnjs.cloudflare.com/ajax/libs/jquery/3.4.1/jquery.min.js" )

      //- Плагин слайдера
      //- script( src="//cdnjs.cloudflare.com/ajax/libs/slick-carousel/1.9.0/slick.min.js" )

      //- Плагин маски ввода
      //- script( src="//cdnjs.cloudflare.com/ajax/libs/jquery.inputmask/3.3.4/jquery.inputmask.bundle.min.js" )

      script( src="scripts/main.min.js" )